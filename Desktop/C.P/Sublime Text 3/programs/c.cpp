#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define mp make_pair
#define pb push_back
int main()
{
   ios_base::sync_with_stdio(0);
   cin.tie(0);
   cout.tie(0);
   #ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
   #endif
   int t;
   cin>>t;
   while(t--)
   {
   	cin>>n;
   	int a[n];
   	for(int i=0;i<n;i++)
   	{
   		cin>>a[i];
   	}
   	int r=(n*(n+1))/2;
   	if(r&1){med=r/2+1;}
   	else{med=r/2;}
   	int size=med/n;int pos=med%n;
   	for(int i=0;i<n;i++)
   	{
      
   	}
   }   
}